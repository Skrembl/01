const burgerMenu = document.getElementsByClassName("burger-menu");
const closeMenu = document.getElementsByClassName("close-menu");
const visibleMenu = document.getElementsByClassName("adaptive");

dispatchEvent.addEventListener('click', () => {
    // visibleMenu.
    // if (visibleMenu) {

    // }
})